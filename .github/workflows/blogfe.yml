name: run_blog_fe

on: 
  push:
    branches: [main]
    
jobs:
  run-blog-fe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run dev
  
